const publicURL = "http://czj.huaian.gov.cn/zbcg/index";
const extension = ".html";

/**
 * 采购意向公开
 */
export const Purchase_Intention_Announcement = publicURL + "1" + extension;

/**
 * 采购（征集）公告
 */
export const Purchase_Solicitation_Announcement = publicURL + "3" + extension;

/**
 * 更正公告
 */
export const Correct_Announcement = publicURL + "4" + extension;

/**
 * 废标（终止）公告
 */
export const Bid_Rejection_Or_Termination_Announcement =
  publicURL + "5" + extension;

/**
 * 结果（入围）公告
 */
export const Results_Or_Shortlisted_Announcement = publicURL + "6" + extension;

/**
 * 合同公告
 */
export const Contract_Announcement = publicURL + "7" + extension;

/**
 * 其他公告
 */
export const Other_Announcement = publicURL + "8" + extension;
